[pytest]
# Anchor the rootdir at the prototype so its tests run self-contained, with the
# prototype dir on the path (not the repo root, which has an unrelated app.py).
testpaths = tests
pythonpath = .
addopts = -q
